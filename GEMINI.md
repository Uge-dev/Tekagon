# Project Mandates (GEMINI.md)

This file contains foundational instructions and project-specific rules for Gemini CLI. These instructions take absolute precedence over general defaults.

## Project Overview
This project, **teckiq website** (Tekagon Scheduler), is a complete scheduling system for Tekagon Digital with REAL email sending capabilities.

## Tech Stack
- **Backend:** Node.js (Express) with SendGrid (`@sendgrid/mail`), CORS, and `node-fetch`.
- **Frontend:** Node.js (Express) with `socket.io` (likely for real-time features).
- **Other:** Both use `nodemon` for development.

## Engineering Standards
- **Naming Conventions:** 
- **Style Guide:** 
- **Type Safety:** 

## Development Workflow
### Build & Run
- **Build Command:** 
- **Run Command (Backend):** `npm run dev` (within `backend/`)
- **Run Command (Frontend):** `npm run dev` (within `frontend/`)

### Testing
- **Test Command:** 
- **Verification Logic:** 

## Critical Instructions
- *Add any specific constraints or mandatory patterns here.*
